using RuneSharp.Core.GrandExchange;
using RuneSharp.RuneScape3;
using Xunit;

namespace RuneSharp.Tests
{
    public class UnitTest1
    {
        [Fact]
        public async void Test1()
        {
            var client = new ApiClient();

//            var item = client.GetItem(4151).Result; // Giver null
//            var items = client.GetItems(ItemCategory.Ammo, 'd').Result;
//            var category = client.GetCategory(ItemCategory.Potions).Result;
//            var image = client.GetItemImage(4151).Result;
//            var sprite = client.GetItemSprite(4151).Result;
//            var graphData = client.GetItemGraphData(4151).Result;
//            var playerStats = client.GetPlayerStats("vestervang").Result;

            var i = 0;
        }
    }
}